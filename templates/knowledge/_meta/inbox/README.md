# Inbox — pendientes de clasificación

Acá caen los documentos nuevos que todavía no tienen lugar en el esqueleto
(los de la carpeta `Knowledge` de Drive y las altas por chat sin destino).

Claw propone la ubicación (`python -m pipeline move <drive_id> <contexto>/<area>`),
los marca en el `_index.md` del área y Daiana valida (RF-21).
